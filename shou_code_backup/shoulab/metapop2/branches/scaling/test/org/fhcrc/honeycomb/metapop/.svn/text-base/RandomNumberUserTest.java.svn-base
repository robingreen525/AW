package org.fhcrc.honeycomb.metapop;

import org.junit.*;
import static org.junit.Assert.*;

@Ignore
public class RandomNumberUserTest {
    private RandomNumberUser rng = new RandomNumberUser(1);

    @Test
    public void rngStream() {
        for (int i=0; i<10; i++) {
            System.out.println(rng.getNextPoisson(10));
        }
    }
}

