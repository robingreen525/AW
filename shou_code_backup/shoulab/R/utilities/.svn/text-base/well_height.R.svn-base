well.height <- function(vol, plate="greiner") {
    if (identical(plate,"greiner") {
    return( (10.9/0.285)*((0.285*height*3/(pi*10.9)+3.195^3)^(1/3)-3.195) )
    } else {
        stop("Don't recognize that type!")
    }
}
